import { ThemeOptions } from '@mui/material';
import { green, orange, purple, red } from '@mui/material/colors';

export const createMuiThemePadrao: ThemeOptions = {
  palette: {
    primary: red,
    secondary: orange,
  },
};
